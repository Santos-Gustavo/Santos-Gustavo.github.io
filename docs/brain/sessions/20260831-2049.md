# 2026-08-31 20:49 — DESIGN-SYSTEM-001

## What changed

Implemented DESIGN-SYSTEM-001: locked the "Paper, Ink, Brass & Forest" visual system across the app as a refinement pass (not a redesign), per a detailed brief the user brought in from another Claude session, with one added constraint from the user: avoid a full visual rewrite of the app flow.

- `styles.css`: corrected 5 of 15 `:root` tokens to the newly locked exact hex values; fixed two IBM Plex Mono typography violations (`.field label`, `.project-filter-btn` — spec forbids Mono for form labels and "ordinary navigation"); added Space Grotesk to `.auth-actions button`.
- `landing-page.html` (previously untracked, on this `LANDING-PAGE-001` branch, still using a generic SaaS blue/orange theme): full restyle onto the shared token system, copy corrected to the locked spec text, fixed an overclaiming wording violation ("Visualizado pelo cliente" → "Visualizado"), registration marks added to the two hero mockup cards only.
- `js/auth/auth.js`, `js/projects/sections/financial.js`: hardcoded off-palette inline colors replaced with design tokens.
- New `tests/e2e/landing-page.spec.js`; added a logout case to `tests/e2e/auth.spec.js` (previously untested).

Full detail, including explicit scope-out decisions (report-renderer.js's generated document left untouched, reset-password.html left unstyled, the separate "Trabalhos Extra" approval wording left alone): `docs/features/DESIGN-SYSTEM-001.md`.

## Why

Product/design locked the exact "Paper, Ink, Brass & Forest" tokens and typography rules; the app's existing styling was close but not exact, and the new landing page hadn't been touched at all.

## Process notes

- Used a clarifying question up front (`AskUserQuestion`) on how `landing-page.html` should relate to `index.html`'s auth flow — confirmed: stays a separate, standalone page, no routing change, no changes to existing E2E assumptions about `/`.
- Went through `EnterPlanMode`/`ExitPlanMode` given the multi-file, cross-cutting scope; plan approved before any edits.
- Writing the required logout E2E coverage (brief's own testing requirement, previously absent) surfaced a real, deterministic pre-existing bug: `signIn()`/`signUp()` each called `showLoggedInUI()` directly, redundant with the `onAuthStateChange` listener already doing so — in a fast logout, the direct call could fire after sign-out completed, re-showing the app shell. Diagnosed with an instrumented reproduction (not guessed), then asked the user how to handle it (fix now / report-only / leave failing) since it meant touching real auth logic, outside the stated visual-only scope. User chose fix-now. Fixed by removing the redundant direct calls; the `onAuthStateChange` listener is now the single source of truth for auth-driven UI updates.

## Verification

- `node scripts/check-share-import-boundary.js` — passed.
- `node scripts/check-esm-migration.js` — passed.
- `npm run test:e2e` — 44/44 passed on the clean re-run. (One run hit an unrelated single timeout in `project-archived-photo-evidence.spec.js`; re-ran in isolation, passed in 5.9s — pre-existing flakiness, not a regression.)

## What's next

- Carried forward from before this session (untouched by this work): CLIENT-SHARE-LINK-001 still needs real Gemini/ChatGPT gate sign-off before Phase 2, a real `evidence.md` entry, and resolution of the concurrent-share-links-per-report open question (REQ-03).
- New from this session: consider whether `report-renderer.js`'s generated client-facing report document should eventually adopt the same tokens (deliberately out of scope here — see DESIGN-SYSTEM-001 §2); do a real-device outdoor/mobile contrast check on the new token values before wide rollout.
