# Session 20260825-2145

## What changed

**1. Audited the new brain/product/features doc structure (committed 2026-08-22) and cleaned it up.**

Found and fixed:
- `CHANGE-ORDER-001` was referenced as a real, "Released" feature in `INDEX.md`, `roadmap.md`, `evidence.md` (fake interview log), and `decisions.md` — but no such feature was ever built in this repo (git log has zero related commits, no feature file exists). It's leftover example content from `OPERATION-MODEL.md` that had bled into files meant to represent real state. Stripped from all of them; the two generalizable principles in `decisions.md` were re-anchored without the fictional provenance line.
- `docs/client-share-link-design.md` — a real, substantial, pre-existing engineering design doc — was orphaned outside the `docs/features/` structure the new navigation rules point to. Promoted it into `docs/features/CLIENT-SHARE-LINK-001.md` under the canonical feature template (Status/Risk/Evidence/Execution Path header, Sections 1–6 + Decision Log), keeping its original engineering content intact as Section 3.
- Missing `docs/brain/context/stack.md` (referenced by `CLAUDE.md`/`soul.md` but never created) — added, based on actual codebase inspection (vanilla JS/ESM, Supabase, Playwright).
- `architecture.md`'s `[frontend framework]` placeholder — filled with the real stack.
- `CLAUDE.md`'s Project State line was still the raw template — initialized.
- `[RAW-BASE-URL]` placeholders in the Gemini/ChatGPT `HOW-TO-USE.md` files — filled with the real raw.githubusercontent.com URL for this repo.

**2. Added the two things the docs structure was missing for actual Gemini/ChatGPT use:**
- `docs/product/features-catalog.md` — a living list of what the product actually ships today (Auth, Projects, Reports, Photos, Clients, Payments), built from real code inspection, not assumption. `GEMINI.md` now fetches it first and must flag any new idea that duplicates, conflicts with, or reverses something already shipped.
- Enriched the Gate 2 (PM Specification) template in both `CHATGPT.md` and the canonical `OPERATION-MODEL.md` §15 with explicit **Task Breakdown**, **Required Tests**, and **Definition of Done** subsections — previously the template only had bare requirements/acceptance criteria, no process/DoD structure. Kept it inside the existing per-feature file (Section 2) rather than splitting into a second per-feature file.

**3. Reviewed and cleaned up `CLIENT-SHARE-LINK-001.md` Sections 1–2** after Gustavo filled them in (apparently via a Gemini/ChatGPT session).

Problems caught and fixed on explicit instruction:
- `CHANGE-ORDER-001` had crept back in as a stated "dependency" in Section 1 Priority — the same fabricated-feature problem from item 1, now appearing in a real Gemini-authored artifact. Removed, replaced with the real priority rationale (professional WhatsApp delivery without manual PDF export).
- Section 2 didn't follow the just-built Gate 2 template — missing User Story, Scope, Out-of-Scope, Required Tests, Definition of Done entirely. Rebuilt to match.
- `AC-01.4` required a per-photo `is_client_visible` flag that doesn't exist in the schema or in Section 3's engineering design, and no task/phase built it — scope mismatch between the PM spec and the engineering design. Removed for v1 per Gustavo's instruction; added a matching Out-of-Scope line.
- Evidence Level 2 claimed with zero backing entry in `docs/product/evidence.md` (which requires traceability). Downgraded to 0, with a note on what would raise it.
- "Value Gate Pass: YES — criteria 2, 3, 5" cited a numbered rubric that doesn't exist anywhere in `GEMINI.md`. Replaced with named rationale bullets.
- "unsaved or draft report" wording assumed a draft state that doesn't exist in `db-reports.js` — confirmed by grep, reworded to "unsaved report without a persisted report id."
- Top-of-file Status/Evidence banner was stale relative to the now-drafted Sections 1–2 — updated.

## Why

The brain/product docs structure is new (committed the same day this thread of work started) and was seeded partly from the generic `OPERATION-MODEL.md` reference doc — some of that example content (`CHANGE-ORDER-001`) wasn't adapted for this specific repo before being treated as live truth, which is exactly the failure mode the whole "chat reasons, files remember" system is supposed to prevent. The Gate 2 template gap (no Definition of Done / Required Tests) was a real omission — ChatGPT's role definition promised more than the template structure actually delivered.

## What's next

- `CLIENT-SHARE-LINK-001` Sections 1–2 are drafted but **not gate-approved** — still needs a real Gemini Value Gate pass and a real ChatGPT Definition Gate pass (not just Claude cleanup) before Phase 2 implementation starts.
- Evidence Level is 0 — needs either a real logged observation in `evidence.md` or an honest re-assessment from Gemini; don't let it silently drift back to "Level 2" without a traceable source.
- Open question, unresolved by design (not by omission): does creating a new share link for a report that already has a live unexpired link invalidate the old one, or can both work concurrently? REQ-03/AC-03.3 doesn't say. Needs a product/eng decision before Phase 2.
- `inbox.md` still has its one example placeholder line (2026-08-22, explicitly labeled "Example") — not yet 2 weeks old, no action needed yet.
- No code has been written for `CLIENT-SHARE-LINK-001` — everything this session was documentation only, per explicit instruction.
