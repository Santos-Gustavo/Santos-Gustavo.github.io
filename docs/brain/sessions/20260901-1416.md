# 2026-09-01 14:16 — LIVE-SITE-FETCH-001: GitHub Pages root fetch investigation

## What happened

ChatGPT's web-fetch tool reported `Failed to fetch https://santos-gustavo.github.io/: Cache miss` while attempting an external design audit, raising concern the root Pages URL wasn't serving correctly after the recent `index.html`/`app.html` routing swap.

## Investigation, in order

1. **Branch/commit state**: on `UX-FIXES-002`, one commit ahead of `main`, already pushed to `origin/UX-FIXES-002` but not merged. The actual routing swap (DESIGN-SYSTEM-001 §6) is a separate, earlier commit already merged to `main` via PR #8 (`f5e1fcb`) — confirmed `origin/main` has the correct root layout (`index.html`, `app.html`, no `landing-page.html`) regardless of UX-FIXES-002's merge state.
2. **Stale-reference sweep**: `git grep` across all HTML/JS/tests/docs for `landing-page.html`/`index.html`/`app.html`/`reset-password.html` — every remaining `landing-page.html` hit is historical documentation, no live references. All functional references correctly target `app.html`.
3. **Live site, direct**: `curl -I` against `/`, `/app.html`, `/reset-password.html` — all three returned `200 OK` right now. Root body is byte-for-byte identical (8889 bytes) to `origin/main`'s `index.html`, contains the exact required headline, no payment wording.
4. **Local server**: `npx serve . -l 4173` — root/CSS/JS all 200; `app.html`/`reset-password.html` 301-redirect locally only (serve's own "clean URLs" feature, already known from earlier sessions, doesn't occur on GitHub Pages).
5. **Full test suite**: static checks pass; `npm run test:e2e` — 59/59 passing (added 2 new landing-page tests: Entrar → app.html reachability, reset-password.html standalone load).

## Conclusion

Category D — external fetch/cache issue on the auditor's side, not reproducible against the live site. A, B, and C were all ruled out directly with evidence, not assumed. Nothing on the repo or Pages hosting side needed to change; the "Cache miss" was most likely a transient tool-side fetch failure or a check made before that day's Pages rebuild had propagated.

## Files changed

- `tests/e2e/landing-page.spec.js` — two new regression tests, plus a stale top-of-file comment fixed (was still describing the pre-routing-swap `landing-page.html` naming).
- `docs/features/LIVE-SITE-FETCH-001.md` (new), `docs/features/INDEX.md`, `CLAUDE.md` — Brain protocol bookkeeping.

## Verification

- `node scripts/check-share-import-boundary.js` — passed.
- `node scripts/check-esm-migration.js` — passed.
- `npm run test:e2e` — 59/59 passed in 2.2 minutes.
- `curl -I` against all three live URLs — 200 OK, verified directly (see `docs/features/LIVE-SITE-FETCH-001.md` §2 for full output).

## What's next

Nothing new. If an external auditor reports a fetch failure again, re-run `curl -I https://santos-gustavo.github.io/` first — if that comes back 200 with fresh content (as it did here), the problem is on the auditor's fetch path, not this site. UX-FIXES-002 still sits unmerged on its own branch — a merge decision is a separate, un-asked-for call, not part of this investigation.
